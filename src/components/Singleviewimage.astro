---

---

<div class="artist-card">
  <img src="/billeder/eminem.webp" alt="Peggy Gou" class="artist-image" />
  <h2 class="artist-name">PEGGY GOU</h2>
</div>

<style>
  /* Styling for container */
  .artist-card {
    position: relative;
    height: 100vh;
    width: 50vh;
    clip-path: polygon(0% 0%, 75% 0%, 100% 50%, 75% 100%, 0% 100%);
    overflow: hidden;
    aspect-ratio: 16 / 9;
  }

  .artist-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .artist-name {
    position: absolute;
    bottom: 30vh;
    left: 3vw;
    color: #ffffff;
    font-size: 2vw;
    font-family: sans-serif;
    letter-spacing: 2px; /
    text-transform: uppercase; 
    z-index: 1; 
    aspect-ratio: 16 / 9;
  }


@media (max-width: 768px) {
  .artist-image {
    width: 450px;
    height: 450px;
    clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
  }

 
  .artist-card {
    flex-direction: column;
    text-align: center;
  }

  .artist-name {
    margin-top: 20px;
  }
}

</style>
